<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.social.userservice.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="com.social.userservice.domain.po.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="nickname" property="nickname" />
        <result column="password" property="password" />
        <result column="email" property="email" />
        <result column="phone" property="phone" />
        <result column="avatar" property="avatar" />
        <result column="gender" property="gender" />
        <result column="birthday" property="birthday" />
        <result column="status" property="status" />
        <result column="last_login_time" property="lastLoginTime" />
        <result column="last_login_ip" property="lastLoginIp" />
        <result column="is_deleted" property="isDeleted" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <!-- 根据用户名查询用户 -->
    <select id="findByUsername" resultMap="BaseResultMap">
        SELECT * FROM "user" WHERE username = #{username} AND is_deleted = 0
    </select>

    <!-- 根据邮箱查询用户 -->
    <select id="findByEmail" resultMap="BaseResultMap">
        SELECT * FROM "user" WHERE email = #{email} AND is_deleted = 0
    </select>

    <!-- 根据手机号查询用户 -->
    <select id="findByPhone" resultMap="BaseResultMap">
        SELECT * FROM "user" WHERE phone = #{phone} AND is_deleted = 0
    </select>

    <!-- 更新最后登录信息 -->
    <update id="updateLoginInfo">
        UPDATE "user"
        SET last_login_time = NOW(), last_login_ip = #{loginIp}
        WHERE id = #{userId} AND is_deleted = 0
    </update>

</mapper>